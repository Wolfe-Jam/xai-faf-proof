# xai-faf-proof

**Grok-flavored performance receipts for `.fafm` / `.fafb` memory.**

Same falsifiable benchmark methodology as the original `faf-memory-proof`, now running on a **10,000-file Grok memory corpus**.

### Headline Result
**1,200× faster** type-filter queries vs `grep`  
on a 10,000-file Grok memory corpus.

... (full README as drafted)